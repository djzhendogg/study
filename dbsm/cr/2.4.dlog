Accepted(ContestId, Letter) :-
    Runs(_, SessionId, Letter, _, 1),
    Sessions(SessionId, _, ContestId, _),
    Problems(ContestId, Letter, _).
Result(ContestId, Letter) :-
    Problems(ContestId, Letter, _),
    ¬ Accepted(ContestId, Letter).
     